---
import { Image } from 'astro:assets';
import logoSrc from '../assets/tk-logga.png';

const produktLinks = [
  { name: 'Funktioner', href: '/#funktioner' },
  { name: 'Priser', href: '/priser' },
  { name: 'Partners', href: '/partners' },
];

const supportLinks = [
  { name: 'Vanliga frågor', href: '/#faq' },
  { name: 'Kontakt', href: '/kontakt' },
];

const legalLinks = [
  { name: 'Integritetspolicy', href: '/integritetspolicy' },
  { name: 'Användarvillkor', href: '/anvandarvillkor' },
];
---

<footer class="bg-gray-900 text-white pt-16 pb-8 relative overflow-hidden">
  <!-- Subtle background pattern -->
  <div class="absolute inset-0 opacity-5">
    <div class="absolute inset-0 bg-repeat" style="background-image: url('/grid-pattern.svg');"></div>
  </div>

  <!-- Animated gradient background -->
  <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-blue-600 via-blue-500 to-blue-600 bg-[length:200%_auto] animate-gradient-x"></div>

  <div class="container-custom relative">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-8 pb-12 border-b border-gray-800">
      <div class="lg:col-span-2">
        <a href="/" class="inline-block transition-transform duration-300 hover:scale-105" aria-label="Taxi Administration hem">
          <Image src={logoSrc} alt="Taxi Administration logotyp" width={120} height={40} />
        </a>
        <p class="mt-4 text-gray-400 max-w-md">
          Automatisera dina löneberäkningar med precision för taxi- och transportföretag. Utvecklat specifikt för svenska skatteregler och regelverket.
        </p>
        <div class="mt-6 flex space-x-4">
          <a
            href="https://www.linkedin.com/company/baerell/"
            target="_blank"
            rel="noopener noreferrer"
            class="text-gray-400 hover:text-white transition-all duration-300 hover:scale-110 transform"
            aria-label="LinkedIn"
          >
            <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z" />
            </svg>
          </a>
        </div>
      </div>

      <div>
        <h3 class="text-lg font-semibold mb-4 text-white/90 hover:text-white transition-colors duration-300">Produkten</h3>
        <ul class="space-y-3">
          {produktLinks.map((link, index) => (
            <li class="transform transition-all duration-300 hover:translate-x-1">
              <a
                href={link.href}
                class="text-gray-400 hover:text-white transition-colors duration-300 inline-block"
                style={`transition-delay: ${index * 50}ms;`}
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold mb-4 text-white/90 hover:text-white transition-colors duration-300">Support</h3>
        <ul class="space-y-3">
          {supportLinks.map((link, index) => (
            <li class="transform transition-all duration-300 hover:translate-x-1">
              <a
                href={link.href}
                class="text-gray-400 hover:text-white transition-colors duration-300 inline-block"
                style={`transition-delay: ${index * 50}ms;`}
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-lg font-semibold mb-4 text-white/90 hover:text-white transition-colors duration-300">Juridiskt</h3>
        <ul class="space-y-3">
          {legalLinks.map((link, index) => (
            <li class="transform transition-all duration-300 hover:translate-x-1">
              <a
                href={link.href}
                class="text-gray-400 hover:text-white transition-colors duration-300 inline-block"
                style={`transition-delay: ${index * 50}ms;`}
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="pt-8 text-center text-gray-400 text-sm space-y-2">
      <p class="hover:text-gray-300 transition-colors duration-300">&copy; {new Date().getFullYear()} taxiadministration.se. Alla rättigheter förbehållna.</p>
      <p class="text-gray-500">Utvecklat av Baerell AB | Stockholm, Sverige</p>
    </div>
  </div>
</footer>
